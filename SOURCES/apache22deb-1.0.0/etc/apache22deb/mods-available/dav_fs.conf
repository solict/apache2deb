<IfModule dav_fs_module>
#
# This module requires the service of mod_dav. It acts as a support module for
# mod_dav and provides access to resources located in the server's file system.
# The formal name of this provider is filesystem. mod_dav backend providers
# will be invoked by using the Dav directive:
#

# Location of the WebDAV lock database.
DAVLockDB ${APACHE_LOCK_DIR}/DAVLock

</IfModule>
